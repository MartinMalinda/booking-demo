

<div class="ui top fixed menu">
  <div class="brand item">
    Mini BookingSync-API-Client
  </div>
</div>

{{#if isAuthenticated}}
  {{outlet}}
  {{else}}
    <div class="ui container">
      <h1>Welcome. Please login to access the system.</h1>
      <button {{action "login"}} class="ui big green button">Login</button>
    </div>
{{/if}}